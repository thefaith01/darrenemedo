# Copy to .env.local for local development.
# When absent, the contact form runs in demo mode: submissions are
# logged to the server console and a success response is returned.

# Resend API key (resend.com -> API Keys). Server-side only.
RESEND_API_KEY=
